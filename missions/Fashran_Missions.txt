fashros_religion_missions = {
	slot = 1
	generic = no
	ai = yes
	potential = {
		NOT = { map_setup = map_setup_random }
		tag = FAS
	}
	has_country_shield = yes

	for_the_clouds = {
		icon = fashros_wheels
		required_missions = { }
		position = 1
		trigger = {
		}
		effect = {
		}
	}
}
fashros_expansion_missions = {
	slot = 3
	generic = no
	ai = yes
	potential = {
		NOT = { map_setup = map_setup_random }
		tag = FAS
	}
	has_country_shield = yes
	
	fas_venture_westward = {
		icon = mission_bedouins
		required_missions = { }
		trigger = {
			is_year	= 215
			NOT = { is_at_war = yes }
		}
		effect = {
			country_event = { id = fashros.1 }
			nuzoto_area = { add_claim = ROOT }
			toza_area = { add_claim = ROOT }
		}
	}
}